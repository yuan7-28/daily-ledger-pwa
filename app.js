const $=s=>document.querySelector(s),money=n=>`¥${(+n).toFixed(2)}`,today=new Date().toISOString().slice(0,10);let displayedMonth=new Date(today.slice(0,7)+'-01T12:00:00'),items=JSON.parse(localStorage.getItem('little-ledger')||'[]');const save=()=>{localStorage.setItem('little-ledger',JSON.stringify(items));render()},active=()=>items.filter(x=>!x.finished),daily=x=>x.price/Math.max(1,x.usedDates.length),used=(x,d=today)=>x.usedDates.includes(d),fmt=d=>new Intl.DateTimeFormat('zh-CN',{month:'long',day:'numeric',weekday:'short'}).format(new Date(d+'T12:00:00'));
function render(){const u=items.filter(x=>used(x)),month=today.slice(0,7),mc=items.reduce((s,x)=>s+x.usedDates.filter(d=>d.startsWith(month)).length*daily(x),0);$('#today-cost').textContent=money(u.reduce((s,x)=>s+daily(x),0));$('#month-cost').textContent=money(mc);$('#today-note').textContent=u.length?`今天记录了 ${u.length} 件`:'今天还没有记录';$('#today-label').textContent=fmt(today);$('#active-count').textContent=`${active().length} 件`;const box=$('#usage-list');box.innerHTML='';$('#empty-usage').hidden=active().length>0;active().forEach(x=>{let e=document.createElement('div');e.className='usage';e.innerHTML=`<span class="dot">${x.name[0]}</span><div><strong>${x.name}</strong><small>已使用 ${x.usedDates.length} 天 · 暂算日均 ${money(daily(x))}</small></div><button class="check ${used(x)?'done':''}">${used(x)?'已记录 ✓':'今天使用'}</button>`;e.querySelector('button').onclick=()=>{x.usedDates=used(x)?x.usedDates.filter(d=>d!==today):[...x.usedDates,today];save()};box.append(e)});renderItems('#active-list',active(),false);const f=items.filter(x=>x.finished);$('#finished-section').hidden=!f.length;renderItems('#finished-list',f,true);calendar();history()}
function renderItems(s,list,finished){const box=$(s);box.innerHTML='';list.forEach(x=>{let e=$('#item-template').content.firstElementChild.cloneNode(true);e.querySelector('h3').textContent=x.name;e.querySelector('.item-meta').textContent=`购于 ${x.purchaseDate} · 实际使用 ${x.usedDates.length} 天`;e.querySelector('.cost-row strong').textContent=`${finished?'最终':'暂算'}日均 ${money(daily(x))}`;e.querySelector('.cost-row span').textContent=finished?`共 ${x.usedDates.length} 个真实使用日`:'用完后自动确认';let b=e.querySelector('.more');b.textContent=finished?'删除':'用完';b.onclick=()=>{if(finished)items=items.filter(i=>i.id!==x.id);else if(!x.usedDates.length)return alert('至少记录一天使用后才能结算哦。');else x.finished=true;save()};box.append(e)})}const entries=()=>items.flatMap(x=>x.usedDates.map(date=>({date,name:x.name,cost:daily(x)})));
function calendar(){const y=displayedMonth.getFullYear(),m=displayedMonth.getMonth(),key=`${y}-${String(m+1).padStart(2,'0')}`,data={};entries().filter(x=>x.date.startsWith(key)).forEach(x=>data[x.date]=(data[x.date]||0)+x.cost);const vals=Object.values(data);$('#month-title').textContent=`${y}年${m+1}月`;$('#month-average').textContent=vals.length?`有记录日均 ${money(vals.reduce((a,b)=>a+b,0)/vals.length)}`:'这个月还没有记录';const g=$('#calendar-grid');g.innerHTML='';const offset=(new Date(y,m,1).getDay()+6)%7,days=new Date(y,m+1,0).getDate();for(let i=0;i<offset;i++)g.append(document.createElement('span'));for(let d=1;d<=days;d++){const date=`${key}-${String(d).padStart(2,'0')}`,e=document.createElement('span');e.className=`day ${data[date]?'used':''} ${date===today?'today':''}`;e.innerHTML=`${d}${data[date]?`<i>¥${Math.round(data[date])}</i>`:''}`;g.append(e)}}
function history(){const by={};entries().forEach(x=>(by[x.date.slice(0,7)]??=[]).push(x));const box=$('#history-list');box.innerHTML='';Object.keys(by).sort().reverse().forEach(month=>{const es=by[month],days={};es.forEach(x=>(days[x.date]??=[]).push(x));let e=document.createElement('article');e.className='history-month';e.innerHTML=`<div class="history-title"><span>${month.replace('-','年')}月</span><span>${money(es.reduce((s,x)=>s+x.cost,0))}</span></div>`;Object.keys(days).sort().reverse().forEach(d=>{let a=days[d],r=document.createElement('div');r.className='history-day';r.innerHTML=`<div>${fmt(d)}<small>${a.map(x=>x.name).join('、')}</small></div><strong>${money(a.reduce((s,x)=>s+x.cost,0))}</strong>`;e.append(r)});box.append(e)});if(!box.children.length)box.innerHTML='<p class="empty">还没有使用历史 🌼</p>'}
$('#add-button').onclick=()=>$('#add-dialog').showModal();$('#add-dialog').addEventListener('close',()=>{if($('#add-dialog').returnValue==='default'){let name=$('#item-name').value.trim(),price=+$('#item-price').value;if(name&&price>0){items.unshift({id:Date.now(),name,price,purchaseDate:today,usedDates:[],finished:false});$('#item-name').value='';$('#item-price').value='';save()}}});$('#prev-month').onclick=()=>{displayedMonth.setMonth(displayedMonth.getMonth()-1);calendar()};$('#next-month').onclick=()=>{displayedMonth.setMonth(displayedMonth.getMonth()+1);calendar()};document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('selected'));document.querySelectorAll('.page').forEach(x=>x.classList.remove('active'));b.classList.add('selected');$('#'+b.dataset.page).classList.add('active');$('#add-button').hidden=b.dataset.page==='calendar-page'});if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js');render();
